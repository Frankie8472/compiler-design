/* test03  Variable name same as label name, label_print, label_new_line, main (must fail) */

class Main {
	void main() {

        // Must fail, special character "_" is not allowed
        int main, label_print, label_new_line;
        label_print = 5;
        label_new_line = 6;
        main = 7;
        write(main);
        writeln();
        write(label_print);
        write(label_new_line);

	}
}
